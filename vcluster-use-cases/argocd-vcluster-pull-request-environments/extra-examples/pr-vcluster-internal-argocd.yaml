apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: pr-vcluster-internal-argocd
  namespace: argocd
spec:
  generators:
    - pullRequest:
        github:
          appSecretName: loft-demo-org-cred
          labels:
            - create-prvcluster
          owner: loft-demos
          repo: cyprine-app
        requeueAfterSeconds: 30
        template:
          metadata: {}
          spec:
            destination: {}
            project: ''
  template:
    metadata:
      name: cyprine-app-pr-{{number}}
    spec:
      destination:
        name: in-cluster
        namespace: p-api-framework
      info:
        - name: PRvCluster Argo CD URL
          value: https://argocd-cyprine-app-pr-{{number}}.us.demo.dev
        - name: Pull Request
          value: 'https://github.com/loft-demos/cyprine-app/pull/{{number}}'
      project: default
      source:
        kustomize:
          namePrefix: cyprine-app-pr-{{number}}-
          patches:
            - patch: |-
                - op: replace
                  path: /spec/prNumber
                  value: '{{number}}'
              target:
                kind: PullRequestEnvironment
                name: vcluster
        path: vcluster-use-cases/argocd-vcluster-pull-request-environments/internal/kustomize
        repoURL: 'https://github.com/loft-demos/cyprine-app.git'
        targetRevision: '{{head_sha}}'
      syncPolicy:
        automated:
          selfHeal: true
